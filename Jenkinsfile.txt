pipeline {
    
	agent any
  


    tools {
        maven  "maven"
    }

    stages {
	
        stage('code clone') {
            steps {
               git branch: 'main', credentialsId: 'Git', url: 'https://github.com/anilkegarla/myrepo.git'
            }
			}

       
	    stage('clean') {
            steps {
              sh 'mvn clean'  
            }
			}
       
        stage('deploy') {
		
            steps {
              sh 'mvn deploy'
		    } 
            }		    		
    }
}	 